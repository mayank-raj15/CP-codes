#include <bits/stdc++.h>
using namespace std;

int main()
{
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	int n, m;
	cin >> n >> m;

	int val[n + 1] = {0};

	for (int i = 2; i <= n; i++)
	{
		val[i] = m - m % b;
	}

	for (int i = 2; i <= n; i++)
	{
		for (int j = 1; j < i && j < )
		}
}

